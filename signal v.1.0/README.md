# Signal

Project overview and setup live here.

## Requirements

The source of truth for product requirements is:

- `signal-requirements.md`

## Current prototype

A lightweight local dashboard lives in `dashboard/`.

Open `dashboard/index.html` in a browser to preview it.
